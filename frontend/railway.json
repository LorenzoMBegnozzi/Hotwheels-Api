{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "export npm_config_cache=/tmp/npm-cache && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
